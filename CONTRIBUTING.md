# Contributing

1. Use Node.js 20 or newer.
2. Run `npm ci`.
3. Run `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build`.
4. Keep new behavior traceable through the audit ledger.
5. Do not add workflows that identify people, read license plates, or make enforcement conclusions.
